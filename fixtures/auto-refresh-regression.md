# 自动刷新回归测试

用于人工验证：在 FoldMark 打开本文件后，通过外部编辑器修改并保存，正文应自动刷新，应用仍可继续操作。

| 检查项 | 预期结果 |
|---|---|
| 外部保存 | 阅读器自动更新正文 |
| 阅读位置 | 内容更新后仍可滚动和跳转 |

```mermaid
flowchart LR
    A[外部文件已保存] --> B[FoldMark 检测文件变化]
    B --> C[安全地重新读取]
    C --> D[正文自动刷新]
```
