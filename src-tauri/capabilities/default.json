{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only core window operations; document access is mediated by typed Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
